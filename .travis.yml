language: ruby
bundler_args: --without production
cache: bundler

rvm:
  - 2.4.2

addons:
  postgresql: "9.6"

script:
  - RAILS_ENV=test bundle exec rake db:create db:migrate --trace
  - bundle exec rake

